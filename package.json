{
  "name": "cwc-content-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json so Vercel installs the server's dependencies for api/index.js. Local dev still runs from server/ (see README.md) -- this file exists for the Vercel build only.",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0",
    "@supabase/supabase-js": "^2.110.6",
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "node-cron": "^4.6.0"
  }
}
